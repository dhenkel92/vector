[sources.in]
type = "generator"
format = "json"
interval = 10

[sources.metrics]
type = "internal_metrics"
scrape_interval_secs = 2

[sinks.stdout]
type = "console"
inputs = ["metrics"]
target = "stdout"
encoding.codec = "json"
